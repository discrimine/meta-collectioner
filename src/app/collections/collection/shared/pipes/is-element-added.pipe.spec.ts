import { IsElementAddedPipe } from './is-element-added.pipe';

describe('IsElementAddedPipe', () => {
    it('create an instance', () => {
        const pipe = new IsElementAddedPipe();
        expect(pipe).toBeTruthy();
    });
});
